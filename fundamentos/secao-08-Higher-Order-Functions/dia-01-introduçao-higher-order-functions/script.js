/*const name = 'Amanda Anderson Aragao';

const candidatos = (name) =>  {
    return { name , email: ''};
};
const arrayNames=['Amanda Anderson Aragão', 'Danilo Oliveira Gomes']
const generateEmail = (names) => {
    return names.join('_');
}
console.log(generateEmail(arrayNames));*/ 

const generateEMail= (name, lastName) =>[
    let newPEsron = {
        name: name
        lastName: lastName,
        email: `${name}_${lastName}@trybe.com`
    }  
    return Newperson;

    }
]

